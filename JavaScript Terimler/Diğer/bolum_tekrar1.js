// Soru 1

/*let sayi = Math.abs(parseInt(prompt("Lütfen bir sayı giriniz")));
sayi += Math.abs(parseInt(prompt("Lütfen bir sayı daha giriniz")));
sayi += Math.abs(parseInt(prompt("Lütfen son bir sayı daha giriniz")));

alert("Girmiş olduğunuz sayıların ortalaması: " + sayi / 3);*/

// Soru 2

/*let kenar1 = Math.abs(parseInt(prompt("Üçgen için 1. kenar ölçüsü yazınız")));
let kenar2 = Math.abs(parseInt(prompt("Üçgen için 2. kenar ölçüsü yazınız")));
let kenar3 = Math.abs(parseInt(prompt("Üçgen için 3. kenar ölçüsü yazınız")));


if (kenar1 == kenar2 && kenar1 == kenar3) {
    alert("Girmiş olduğunuz verilere göre bu bir EŞ KENAR ÜÇGEN")
} else if (kenar1 == kenar2 || kenar1 == kenar3 || kenar2 == kenar3) {
    alert("Girmiş olduğunuz verilere göre bu bir İKİZ KENAR ÜÇGEN")
}else {
    alert("Girmiş olduğunuz verilere göre bu bir ÇEŞİT KENAR ÜÇGEN")
}*/


/*
let not = parseInt(prompt("Vize notunuzu giriniz.")) * 0.4;
not += parseInt(prompt("Final notunuzu giriniz")) * 0.6;


alert(not >= 50 ? "Tebrikler geçtiniz notunuz: "+ not : "Tühh be önündeki maçlara bakarsın artık: "+ not)
*/

/*
for (let i = 0; i < 5 ; i++) {
    console.log("Ömer Ölgün")
}
let w=0;
while ( w<5){
    console.log("Ömer Ölgün")
        w++
}
let dow=0;
do {
    console.log("Ömer Ölgün")
    dow++
}while(dow<5)*/
/*let say=0;
for (let i =1; i < 100; i++) {
    say += i;
    console.log(say)
}*/
/*

let yazı="";
for (let i =1; i <= 10; i++) {
    yazı += i+",";
}
console.log(yazı)
*/

/*
for (let i = 1; i <= 10; i++) {
    for (let j = 1; j <= 10; j++) {
        sonuc = i * j;
        document.write(i + " X " + j + " = " + sonuc + "<br>");
    }
    document.write("<br><br>")
}
*/


/*let sistem = parseInt(Math.random() * 100);

let user = Math.abs(parseInt(prompt("Lütfen bir sayı giriniz")))

let deneme=0;

for (let i = 0; i < 1;) {
    if (sistem == user) {
        alert("Tebrikler BİLDİNİZ \n "+deneme+" Denemede bildiniz.")
        i++;
    } else if (sistem > user) {
        user = Math.abs(parseInt(prompt("Daha BÜYÜK bir sayı giriniz")))
    } else {
        user = Math.abs(parseInt(prompt("Daha KÜÇÜK bir sayı giriniz")))
    }
    deneme++
}*/

























